node_modules
.next
out
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
# Whole-directory ignore; safe even when `data` is a symlink to another
# worktree's DB (dev setup). Individual entries below stay for documentation.
/data
data/cache.db
data/cache.db-journal
data/cache.db-wal
data/cache.db-shm
data/.session-secret
data/seed.db
data/seed.db.gz

# TypeScript incremental build cache — regenerated per-machine on each build,
# differs across checkouts. Never commit.
*.tsbuildinfo
